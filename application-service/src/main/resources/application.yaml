spring:
  application:
    name: application-service

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    username: app
    password: app
    url: jdbc:postgresql://localhost:5432/test_app

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-uy1xbyu5.us.auth0.com/

server:
  port: 8100

eureka:
  instance:
    hostname: localhost

auth0:
  audience: https://dev-uy1xbyu5.us.auth0.com/api/v2/